# このセッションが本プロジェクト外に作ったもの。片付け方を作った時点で書く。
# 残してあるものは「今後も使うため」であって、忘れているのではない。

# 2026-09-16 22:44  rustup / Rust 1.98.1 を ~/.cargo, ~/.rustup に導入
#   rustup self uninstall
#   → 開発に要るので残している。他の作業でも使うなら消す理由はない。

# 2026-09-16 22:47  lima インスタンス alpine-arm64 (vz, 4cpu/8GB/40GB)
#   limactl stop alpine-arm64 && limactl delete alpine-arm64
#   → 回復環境のビルドと標的の両方で使う。残している。

# 2026-09-17 00:35  lima インスタンス freebsd-arm64 (qemu, 4cpu/8GB/40GB)
#   limactl stop freebsd-arm64 && limactl delete freebsd-arm64
#   → BSD 側の検証に使う。残している。
#   vm-type は qemu であること。vz は Linux 専用で FreeBSD は起動しない。

# 2026-09-17 00:5x  VM の中に作った作業ディレクトリ（消すなら）
#   alpine-arm64:  ~/recovery-build ~/sarachi-build ~/gpt-verify ~/erase-test
#                  ~/timing-test ~/fw-curated ~/fw-laptop ~/libhb-probe
#   freebsd-arm64: ~/libhb-probe ~/libkrimes-patch
#   → いずれも VM ごと消せば無くなる。

# 2026-09-16 22:30〜  scratchpad（Himmelblau の clone、試験イメージ、
#   署名鍵、ESP、qemu のディスクなど 275MB）
#   → セッション終了で消える。
#   ただし scratchpad/dist/signing.key は試験用に生成した署名鍵で、
#   本番の鍵ではない。消えて困らない。

# 2026-09-17 16:xx  当て物の検査で入れたもの
#   rustup toolchain uninstall nightly     # -Z build-std に要った nightly
#   rustup target remove x86_64-unknown-netbsd x86_64-unknown-freebsd aarch64-unknown-freebsd
#   → いずれも当て物の再検査に要るので残している。
#   scratchpad の bsdboxes（DragonFly の image 322M）と patchtest は
#   セッション終了で消える。

# 2026-09-18 13:40  容量が逼迫したので、このセッションの作った物を整理した
#   scratchpad         5.4GB 解放（qemu のディスク、署名した試験用の配布物、
#                      Himmelblau の clone、当て物の検査に使った木）
#   ~/.lima            26GB 解放（alpine-arm64 10G と freebsd-arm64 16G を削除）
#   ~/.cargo/registry  459MB 解放（再取得できるキャッシュ）
#
#   残しているもの:
#   ~/Library/Caches/lima  5.4GB  VM を作り直す時の再取得を避けるため。
#                                 これを消すと次回 image を落とし直す。
#   ~/.rustup              1.4GB  nightly は当て物の再検査（-Z build-std）に要る。
#                                 stable は普段の開発に要る。
#
#   VM の作り直し方:
#     limactl create --name=alpine-arm64 --vm-type=vz --arch=aarch64 \
#       --cpus=4 --memory=8 --disk=40 --tty=false template://alpine-3.23
#     limactl create --name=freebsd-arm64 --vm-type=qemu --arch=aarch64 \
#       --cpus=4 --memory=8 --disk=40 --tty=false template://freebsd-15
#   その後 recovery-env/alpine/build.sh と bundle.sh で回復環境は再現できる。

# 2026-09-22 00:11  lima インスタンス freebsd-arm64 (qemu, 4cpu/8GB/40GB)
#   → 2026-09-22 15:55 に削除済み。geli を実機で確かめるために立てたが、
#     CI が FreeBSD で ci/crypt-real.sh を走らせるようになったので役目を
#     終えた。VM の中に置いた物も一緒に消えている。
#   cgd (NetBSD) や softraid crypto (OpenBSD) に進むときは、それぞれの
#   VM を立てることになる。立て方は下の「VM の作り直し方」と同じ形。

# 2026-09-22  DragonFly 向けの rustc を netbsd-ci-images の release に置いた
#   gh release delete dragonfly-rust --repo zakinko/netbsd-ci-images
#   → 消さない。CI の dragonfly job がここから取って本体を試験している。
#     消すと DragonFly の試験が止まる。1.86 と 1.87 は chain の中間生成物で、
#     次に版を上げるときの種になる。
